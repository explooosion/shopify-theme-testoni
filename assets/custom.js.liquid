$( document ).ready(function() {
    	$.ajax({
            url: 'https://api.ipstack.com/check?access_key=1064c5485963a2fe844a3d485b1b339a',   
            dataType: 'jsonp',
          	async: false,
            success: function(json) {
                var dataResponse = json;
              	if(dataResponse.continent_code=='AS'){
	        		if(dataResponse.country_code=='JP'){
	        			$('input[value="Asia"]+.SizeSwatch').remove();
				   		$('input[value="Europe"]').trigger( "click" );	        			
	        		}else{
	        			$('input[value="Europe"]+.SizeSwatch').remove();
                      	$('input[value="Europa"]+.SizeSwatch').remove(); //Europe
                      	$('input[value="Asia"]').trigger( "click" );
                      
                      	$('input[value="歐洲"]+.SizeSwatch').remove(); //tw-cn
                      	$('input[value="亞洲"]').trigger( "click" );
                      
                      	$('input[value="欧洲"]+.SizeSwatch').remove(); //zh-cn
                      	$('input[value="亚洲"]').trigger( "click" );
                      
                      	$('input[value="유럽"]+.SizeSwatch').remove(); //KO
                      	$('input[value="아시아"]').trigger( "click" );
                      	
                      
	        		}
	        	}else if(dataResponse.continent_code=='EU'){	        		
	        		$('input[value="Asia"]+.SizeSwatch').remove();
				   	$('input[value="Europe"]').trigger( "click" );
				  
				}else if(dataResponse.continent_code=='NA' || dataResponse.continent_code=='SA'){
					if(dataResponse.country_code=='US'){
	        			$('input[value="Asia"]+.SizeSwatch').remove();
				   		$('input[value="Europe"]').trigger( "click" );	        			
	        		}else{
	        			$('input[value="Europe"]+.SizeSwatch').remove();
				   		$('input[value="Asia"]').trigger( "click" );
				   	}
	        	}else{
	        		$('input[value="Asia"]+.SizeSwatch').remove();
				   	$('input[value="Europe"]').trigger( "click" );
	        	}

            }
        });
        
    });
     